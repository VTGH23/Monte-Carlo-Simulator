from setuptools import setup

setup(student_name = 'Victor Teelucksingh',
      project_name='Monte Carlo Simulator',
      version='0.1',
      description='Create, play, and analyze Die games.',
      packages=['montecarlo_dir'])